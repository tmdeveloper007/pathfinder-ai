# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches:
      - "main"
      - "#1412"
      - "#1413"
      - "#1414"
      - "#1415"
      - "#1416"
  pull_request:
    branches:
      - "main"
      - "#1412"
      - "#1413"
      - "#1414"
      - "#1415"
      - "#1416"

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - "5432:5432"
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    strategy:
      matrix:
        node-version: [22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - name: Check for pending Prisma migrations
      env:
        DATABASE_URL: ${{ secrets.CI_DATABASE_URL }}
        SHADOW_DATABASE_URL: ${{ secrets.CI_SHADOW_DATABASE_URL }}
      run: |
        if [ -z "$SHADOW_DATABASE_URL" ]; then
          echo "SHADOW_DATABASE_URL is not set. Skipping migration check."
        else
          # Fail if schema.prisma differs from migrations on disk
          npx prisma migrate diff --from-migrations ./prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url "$SHADOW_DATABASE_URL" --exit-code
        fi
    - name: Generate Prisma client
      run: npx prisma generate
      env:
        DATABASE_URL: postgresql://test:test@localhost:5432/test
    - name: Lint
      run: npm run lint
    - run: npx playwright install --with-deps chromium
    - name: Run tests
      run: npm run test:unit
      env:
        NODE_ENV: test
        DATABASE_URL: postgresql://test:test@localhost:5432/test
        ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    - run: npm run build --if-present
      env:
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_ZHVtbXkuY2xlcmsuYWNjb3VudHMuZGV2JA
        CLERK_SECRET_KEY: sk_test_dummy
    - run: npm run test:e2e
      env:
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_ZHVtbXkuY2xlcmsuYWNjb3VudHMuZGV2JA
        CLERK_SECRET_KEY: sk_test_dummy
